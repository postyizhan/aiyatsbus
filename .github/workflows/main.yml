name: Build
on:
  push:
    branches: [ "dev/1.21" ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - name: Set up JDK 17
        uses: actions/setup-java@main
        with:
          java-version: 17
          distribution: temurin
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@main
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Build
        run: ./gradlew build

      - name: Upload
        uses: actions/upload-artifact@main
        with:
          name: build
          path: plugin/build/libs/Aiyatsbus-*.jar
